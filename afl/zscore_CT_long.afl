#include_once <stir_spread.afl>
#include <z_score.afl>
#include <volatility_filter.afl>

zs = zscore(ROC(C,1), lookback);

Buy =  high_volatility_filter && 
  volume_filter && 
  Cross(zs, -1);
  
Sell = Short = Cover = False;