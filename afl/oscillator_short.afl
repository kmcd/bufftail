#include <stir_spread.afl>
#include <volatility_filter.afl>


lookback = 10;//Optimize("lb",2,2,10,2);

Short = volume_filter && volatility_filter  && Cross( 30, RSI(lookback) ) ;
Buy = Sell = Cover = False;