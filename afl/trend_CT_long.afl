#include_once <stir_spread.afl>
#include <z_score.afl>
#include <volatility_filter.afl>

down_today = ROC(C,1) < 0 
	&& ROC(O,1) < 0 
	&& ROC(H,1) < 0 
	&& ROC(L,1) < 0; 

down_trend = C <= MA(C,lookback);

Buy =   volume_filter && 
  down_today && 
  down_trend;
  
Short = Cover = Sell = False;
