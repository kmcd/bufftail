#include_once <stir_spread.afl>
#include <z_score.afl>
#include <volatility_filter.afl>

momentum = LinRegSlope(ROC(C/O,lookback),lookback);

Buy = volatility_filter &&
	volume_filter && 
	Cross( momentum, 0 ) ;

Sell = Short = Cover = False;

Plot(momentum, "MO", colorYellow);