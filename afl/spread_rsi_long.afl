#include_once <stir_spread.afl>
#include <z_score.afl>
#include <volatility_filter.afl>

#include_once <z_score.afl>

spread_contract = ParamStr("Spread", "EZ#C");
spread = C/Foreign(spread_contract,"C");

//PositionScore = -RSIa(spread, lookback);

Buy = volatility_filter &&  volume_filter && Cross( RSIa(spread, lookback), 30 );
Short = Sell = Cover = False;