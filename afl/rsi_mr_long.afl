#include <stir_spread.afl>
#include <volatility_filter.afl>


Buy = volatility_filter &&  Cross( RSI(lookback), Optimize("entry", 20, 20, 40, 5 ) ) && MA(V, 10) > 1000 ;
Sell = Short = Cover = False;