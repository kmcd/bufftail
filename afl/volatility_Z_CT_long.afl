#include <stir_spread.afl>
#include <volatility_filter.afl>
#include <z_score.afl>

lookback = 10;//Optimize("lb", 10, 5, 10, 1);
zz = zscore(ROC(C,1),lookback) ;
zs = zscore(StDev(C,lookback), lookback) ;
zcomb = (zz+zs)/2;
PositionScore = -zcomb;

Buy =  high_volatility_filter &&  volume_filter && Cross( -1, zcomb );
Short = Sell = Cover = False;