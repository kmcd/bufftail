#include <stir_spread.afl>
#include <volatility_filter.afl>

#include <z_score.afl>

zv = zscore( StDev( ROC(C,1), lookback), lookback);
volatility_filter = abs(zv) < 1;

Buy = volatility_filter &&  volume_filter && volatility_filter && C >= HHV(C,lookback);
Short = Sell = Cover = False;