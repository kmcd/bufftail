#include_once <stir_spread.afl>
#include <z_score.afl>
#include <volatility_filter.afl>


lookback = 10;
volatility_increacing = Cross( StDev(C,lookback) , Optimize("vol", 0.1, 0.02, 0.1, 0.02) ) ;
uptrend = ROC( C, 10 ) > 0;

Buy = volatility_filter &&  uptrend && volatility_increacing;
Short = Sell = Cover = False;