#include <stir_spread.afl>
#include <volatility_filter.afl>
#include <z_score.afl>

lookback = 10;//Optimize("lb", 10, 4, 10, 2);
zz = zscore(ROC(C,1), lookback);
//PositionScore = zz;

Short = high_volatility_filter && volume_filter && Cross(1, zz) ;
Buy = Sell = Cover = False;