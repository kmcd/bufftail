#include <stir_spread.afl>
#include <volatility_filter.afl>

lookback = Optimize("lb", 10, 5, 10, 1);

Buy = volatility_filter &&  Cross(RSI(lookback),70) && StDev(C,lookback) >= 0.05;
Short = Sell = Cover = False;